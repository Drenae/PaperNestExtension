name: papernestextension
description: PaperNest custom Flet controls
version: 1.1.0
publish_to: none

environment:
  sdk: '>=3.2.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flet: ^0.85.3
  flutter:
    sdk: flutter

  flutter_colorpicker: ^1.0.3
  desktop_drop: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
